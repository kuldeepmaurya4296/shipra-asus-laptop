// Shipra Backend Configuration
// Rename this file to .env locally

// Port for the backend server (Use 5000 to avoid conflicts with React Native 8081)
PORT=5000

// Feature Flags (true/false)
USE_MOCK_MAPS=true
USE_MOCK_AUTH=true

// JWT Secret (Change in production)
JWT_SECRET=dev_secret_key_shipra_2024

// Optional Real API Keys (Leave empty to use built-in mocks)
GOOGLE_MAPS_KEY=
TWILIO_SID=
